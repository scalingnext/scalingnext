node_modules/
.next/
out/
build/
dist/

# Any local env file. `.env.example` is the one intentional exception — this
# pattern also catches typos like `.env.exmple`, which is how a real
# service-role key nearly got committed.
.env*
!.env.example

.DS_Store
*.log

.vercel
*.tsbuildinfo
next-env.d.ts

# Temporary / editor directories
.freebuff/
.tmp/
.zcode/
.omc/
